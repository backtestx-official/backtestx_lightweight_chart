{
  const _0x0cca1d = "74c626c9185e36fa";
  let _0x0faa63 = Math.floor(Math.random() * 811);
  const _0x568e4a = Array.from({length: 3}, (_, i) => i + 811).reduce((acc, val) => acc + val, 0);
  if (_0x0faa63 < 0) { console.log(_0x0cca1d); }
  (function() { return _0x568e4a > 0 ? _0x0cca1d : ""; })();
}
(function(window) { if (!window.BacktestxChartCore) { console.error("BacktestxChartCore bundle not loaded."); return; } class BacktestxChart extends window.BacktestxChartCore { constructor(containerId, options) { super(containerId, options); const scaleType = options.horizontalScale || 'time'; const registeredScale = window.ChartingAPI ? window.ChartingAPI.getHorizontalScale(scaleType) : null; if (registeredScale) { this.horizontalScale = registeredScale; } else if (scaleType === 'yield-curve') { this.horizontalScale = window.YieldScale; } else if (scaleType === 'strike-price') { this.horizontalScale = window.StrikeScale; } else { this.horizontalScale = window.TimeScale; } if (scaleType !== 'time' && this.chartType === 'candlestick') { this.customDrawCandles = window.ChartingAPI ? window.ChartingAPI.getCandleRenderer('line') : null; } this._primitives = []; console.log(`⚖️ [BacktestxChart] Initialized with scale: ${scaleType}`); } attachPrimitive(primitive) { if (!this._primitives) { this._primitives = []; } if (!this._primitives.includes(primitive)) { this._primitives.push(primitive); if (typeof primitive.attached === 'function') { primitive.attached({ chart: this, requestUpdate: () => this.render() }); } this.render(); } } detachPrimitive(primitive) { if (this._primitives) { const idx = this._primitives.indexOf(primitive); if (idx !== -1) { this._primitives.splice(idx, 1); if (typeof primitive.detached === 'function') { primitive.detached(); } this.render(); } } } barToX(i) { return (this.horizontalScale || window.TimeScale).barToX(this, i); } xToBar(x) { return (this.horizontalScale || window.TimeScale).xToBar(this, x); } getChartHeight() { const H = this.logicalHeight; const activeIndicators = (this.indicators || []).map(ind => { const config = window.ChartingAPI ? window.ChartingAPI.getIndicator(ind.type) : null; return { ind, config }; }).filter(x => x.config); const paneIndicators = activeIndicators.filter(x => x.config.type === 'pane'); const SUB_PANEL_H = 80; const numPanels = paneIndicators.length; const totalPanelsH = numPanels * SUB_PANEL_H; return Math.max(100, H - this.paddingBottom - totalPanelsH); } priceToY(p, minPrice, maxPrice) { if (minPrice === undefined || maxPrice === undefined) { const mm = this.getVisibleMinMax(); minPrice = mm.minPrice; maxPrice = mm.maxPrice; } return window.DecodedScale.priceToY(this, p, minPrice, maxPrice); } yToPrice(y, minPrice, maxPrice) { if (minPrice === undefined || maxPrice === undefined) { const mm = this.getVisibleMinMax(); minPrice = mm.minPrice; maxPrice = mm.maxPrice; } return window.DecodedScale.yToPrice(this, y, minPrice, maxPrice); } getVisibleRange() { return (this.horizontalScale || window.TimeScale).getVisibleRange(this); } getVisibleMinMax() { return window.DecodedScale.getVisibleMinMax(this); } render() { const ctx = this.ctx; const W = this.logicalWidth; const H = this.logicalHeight; const chartW = W - this.paddingRight; const activeIndicators = (this.indicators || []).map(ind => { const config = window.ChartingAPI ? window.ChartingAPI.getIndicator(ind.type) : null; return { ind, config }; }).filter(x => x.config); const paneIndicators = activeIndicators.filter(x => x.config.type === 'pane'); const overlayIndicators = activeIndicators.filter(x => x.config.type === 'overlay'); const SUB_PANEL_H = 80; const numPanels = paneIndicators.length; const totalPanelsH = numPanels * SUB_PANEL_H; const chartH = Math.max(100, H - this.paddingBottom - totalPanelsH); ctx.clearRect(0, 0, W, H); const isLight = document.body.classList.contains('light-theme'); ctx.fillStyle = isLight ? '#ffffff' : '#131722'; ctx.fillRect(0, 0, W, H); const psSettings = window.ChartingAPI && typeof window.ChartingAPI.getPriceScaleSettings === 'function' ? window.ChartingAPI.getPriceScaleSettings() : null; const tsSettings = window.ChartingAPI && typeof window.ChartingAPI.getTimeScaleSettings === 'function' ? window.ChartingAPI.getTimeScaleSettings() : null; if (psSettings && psSettings.axisBackgroundColor) { ctx.fillStyle = psSettings.axisBackgroundColor; ctx.fillRect(chartW, 0, this.paddingRight, H); } if (tsSettings && tsSettings.axisBackgroundColor) { ctx.fillStyle = tsSettings.axisBackgroundColor; ctx.fillRect(0, chartH + totalPanelsH, W, this.paddingBottom); } if (this.bars.length === 0) return; const { minPrice, maxPrice } = this.getVisibleMinMax(); window.DecodedScale.drawPriceScale(this, ctx, minPrice, maxPrice, chartW); if (this._primitives && this._primitives.length > 0) { this._primitives.forEach(p => { if (typeof p.drawPriceScale === 'function') { try { ctx.save(); p.drawPriceScale(ctx, this.paddingRight, chartH); ctx.restore(); } catch (e) { console.error("Error drawing price scale primitive:", e); } } }); } (this.horizontalScale || window.TimeScale).drawTimeScale(this, ctx, chartH + totalPanelsH); if (this._primitives && this._primitives.length > 0) { this._primitives.forEach(p => { if (typeof p.drawTimeScale === 'function') { try { ctx.save(); p.drawTimeScale(ctx, chartW, this.paddingBottom); ctx.restore(); } catch (e) { console.error("Error drawing time scale primitive:", e); } } }); } ctx.save(); ctx.beginPath(); ctx.rect(0, 0, chartW, chartH); ctx.clip(); if (window.Watermarks && typeof window.Watermarks.draw === 'function') { window.Watermarks.draw(this, ctx, chartW, chartH); } if (this._primitives && this._primitives.length > 0) { this._primitives.forEach(p => { if (typeof p.drawBackground === 'function') { try { ctx.save(); p.drawBackground(ctx, chartW, chartH); ctx.restore(); } catch (e) { console.error("Error drawing background primitive:", e); } } }); } ctx.restore(); const { startIndex: rawStart, endIndex: rawEnd } = this.getVisibleRange(); const len = this.bars.length; const startIndex = Math.max(0, Math.min(len - 1, rawStart)); const endIndex = Math.max(0, Math.min(len - 1, rawEnd)); const cs = this.options?.chartSettings?.symbol || {}; const T = { bullColor: cs.bodyBull || '#26a69a', bearColor: cs.bodyBear || '#ef5350' }; if (this.customDrawCandles) { const slot = this.candleWidth + this.candleGap; const bodyW = this.candleWidth; const visibleBars = this.bars.slice(startIndex, endIndex + 1); const xOffset = this.barToX(startIndex) - slot / 2; this.customDrawCandles( ctx,  visibleBars,  slot,  bodyW,  chartH,  (p) => this.priceToY(p, minPrice, maxPrice),  T,  xOffset,  this ); } else { ctx.save(); const showBody = cs.showBody !== false; const showBorders = cs.showBorders !== false; const showWick = cs.showWick !== false; const bodyBull = cs.bodyBull || '#26a69a'; const bodyBear = cs.bodyBear || '#ef5350'; const borderBull = cs.borderBull || '#26a69a'; const borderBear = cs.borderBear || '#ef5350'; const wickBull = cs.wickBull || '#26a69a'; const wickBear = cs.wickBear || '#ef5350'; const bodyW = this.candleWidth; if (showWick) { ctx.strokeStyle = wickBull; ctx.lineWidth = 1; ctx.beginPath(); for (let i = startIndex; i <= endIndex; i++) { const bar = this.bars[i]; if (bar.close >= bar.open) { const x = this.barToX(i); const yHigh = this.priceToY(bar.high, minPrice, maxPrice); const yLow = this.priceToY(bar.low, minPrice, maxPrice); ctx.moveTo(x, yHigh); ctx.lineTo(x, yLow); } } ctx.stroke(); ctx.strokeStyle = wickBear; ctx.beginPath(); for (let i = startIndex; i <= endIndex; i++) { const bar = this.bars[i]; if (bar.close < bar.open) { const x = this.barToX(i); const yHigh = this.priceToY(bar.high, minPrice, maxPrice); const yLow = this.priceToY(bar.low, minPrice, maxPrice); ctx.moveTo(x, yHigh); ctx.lineTo(x, yLow); } } ctx.stroke(); } if (showBody) { ctx.fillStyle = bodyBull; ctx.beginPath(); for (let i = startIndex; i <= endIndex; i++) { const bar = this.bars[i]; if (bar.close >= bar.open) { const x = this.barToX(i); const yOpen = this.priceToY(bar.open, minPrice, maxPrice); const yClose = this.priceToY(bar.close, minPrice, maxPrice); const bodyTop = Math.min(yOpen, yClose); const bodyH = Math.max(1, Math.abs(yClose - yOpen)); ctx.rect(x - bodyW / 2, bodyTop, bodyW, bodyH); } } ctx.fill(); ctx.fillStyle = bodyBear; ctx.beginPath(); for (let i = startIndex; i <= endIndex; i++) { const bar = this.bars[i]; if (bar.close < bar.open) { const x = this.barToX(i); const yOpen = this.priceToY(bar.open, minPrice, maxPrice); const yClose = this.priceToY(bar.close, minPrice, maxPrice); const bodyTop = Math.min(yOpen, yClose); const bodyH = Math.max(1, Math.abs(yClose - yOpen)); ctx.rect(x - bodyW / 2, bodyTop, bodyW, bodyH); } } ctx.fill(); } if (showBorders) { ctx.strokeStyle = borderBull; ctx.lineWidth = 1; ctx.beginPath(); for (let i = startIndex; i <= endIndex; i++) { const bar = this.bars[i]; if (bar.close >= bar.open) { const x = this.barToX(i); const yOpen = this.priceToY(bar.open, minPrice, maxPrice); const yClose = this.priceToY(bar.close, minPrice, maxPrice); const bodyTop = Math.min(yOpen, yClose); const bodyH = Math.max(1, Math.abs(yClose - yOpen)); ctx.rect(x - bodyW / 2, bodyTop, bodyW, bodyH); } } ctx.stroke(); ctx.strokeStyle = borderBear; ctx.beginPath(); for (let i = startIndex; i <= endIndex; i++) { const bar = this.bars[i]; if (bar.close < bar.open) { const x = this.barToX(i); const yOpen = this.priceToY(bar.open, minPrice, maxPrice); const yClose = this.priceToY(bar.close, minPrice, maxPrice); const bodyTop = Math.min(yOpen, yClose); const bodyH = Math.max(1, Math.abs(yClose - yOpen)); ctx.rect(x - bodyW / 2, bodyTop, bodyW, bodyH); } } ctx.stroke(); } ctx.restore(); } window.Drawings.drawTrendlines(this, ctx, minPrice, maxPrice); window.Drawings.drawActivePreview(this, ctx, minPrice, maxPrice); overlayIndicators.forEach(x => { try { const values = x.config.calculate(this.bars, x.ind.params); ctx.save(); x.config.render(ctx, this, values, { startIndex, endIndex, chartW, chartH }, x.ind.color); ctx.restore(); } catch (e) { console.error(`Error rendering overlay indicator ${x.ind.type}:`, e); } }); if (this._primitives && this._primitives.length > 0) { this._primitives.forEach(p => { if (typeof p.draw === 'function') { try { ctx.save(); p.draw(ctx, chartW, chartH); ctx.restore(); } catch (e) { console.error("Error drawing foreground primitive:", e); } } }); } const watermarkLogoSettings = window.ChartingAPI && typeof window.ChartingAPI.getWatermarkLogoSettings === 'function' ? window.ChartingAPI.getWatermarkLogoSettings() : null; if (watermarkLogoSettings && watermarkLogoSettings.show !== false) { if (this.logoUrl !== watermarkLogoSettings.logoUrl) { this.logoUrl = watermarkLogoSettings.logoUrl; this.logoLoaded = false; this.logoImg = new Image(); this.logoImg.crossOrigin = "anonymous";  this.logoImg.onload = () => { this.logoLoaded = true; this.render(); }; this.logoImg.onerror = () => { console.error("Failed to load watermark logo image:", this.logoUrl); }; this.logoImg.src = this.logoUrl; } if (this.logoLoaded && this.logoImg) { ctx.save(); const badgeX = 12; const badgeY = chartH - 46; const badgeH = 34; const logoSize = 18; const radius = 17;  ctx.font = 'bold 12px Inter, Arial, sans-serif'; const text = watermarkLogoSettings.text || 'Chart by BacktestX'; const textWidth = ctx.measureText(text).width; const expandedW = 10 + logoSize + 8 + textWidth + 12; this.watermarkExpandedWidth = expandedW; const progress = this.watermarkProgress ?? 0; const badgeW = badgeH + progress * (expandedW - badgeH); const isDarkTheme = !isLight; ctx.fillStyle = isDarkTheme ? 'rgba(28, 30, 36, 0.75)' : 'rgba(255, 255, 255, 0.85)'; ctx.strokeStyle = isDarkTheme ? 'rgba(255, 255, 255, 0.15)' : 'rgba(0, 0, 0, 0.15)'; ctx.lineWidth = 1; ctx.beginPath(); if (ctx.roundRect) { ctx.roundRect(badgeX, badgeY, badgeW, badgeH, radius); } else { ctx.rect(badgeX, badgeY, badgeW, badgeH); } ctx.fill(); ctx.stroke(); ctx.save(); ctx.beginPath(); if (ctx.roundRect) { ctx.roundRect(badgeX, badgeY, badgeW, badgeH, radius); } else { ctx.rect(badgeX, badgeY, badgeW, badgeH); } ctx.clip(); const collapsedLogoX = badgeX + (badgeH - logoSize) / 2; const expandedLogoX = badgeX + 10; const logoX = collapsedLogoX + progress * (expandedLogoX - collapsedLogoX); const logoY = badgeY + (badgeH - logoSize) / 2; try { ctx.drawImage(this.logoImg, logoX, logoY, logoSize, logoSize); } catch (e) { console.error("Error drawing watermark logo:", e); } ctx.fillStyle = isDarkTheme ? '#ffffff' : '#131722'; ctx.textAlign = 'left'; ctx.textBaseline = 'middle'; ctx.globalAlpha = Math.max(0, Math.min(1, (progress - 0.2) / 0.8)); ctx.fillText(text, expandedLogoX + logoSize + 8, badgeY + badgeH / 2); ctx.restore();  ctx.restore();  } } ctx.restore(); ctx.save(); ctx.beginPath(); ctx.rect(0, 0, chartW - 5, this.logicalHeight); ctx.clip(); ctx.textBaseline = 'top'; ctx.textAlign = 'left'; const statusHoverBarIdx = this.isMouseOver ? this.xToBar(this.mousePos.x) : this.bars.length - 1; const activeBar = (statusHoverBarIdx >= 0 && statusHoverBarIdx < this.bars.length) ? this.bars[statusHoverBarIdx] : this.bars[this.bars.length - 1]; if (activeBar) { const isUp = activeBar.close >= activeBar.open; const textColor = isLight ? '#131722' : '#d1d4dc'; const labelColor = isLight ? '#707584' : '#9b9ba3'; const bullColor = '#26a69a'; const bearColor = '#ef5350'; const ohlcColor = isUp ? bullColor : bearColor; const isMobile = ('ontouchstart' in window) || (navigator.maxTouchPoints > 0) || (chartW < 500); let curX = 10; let curY = 10; const drawText = (txt, isBold, fillStyle) => { ctx.font = (isBold ? 'bold ' : '') + (isMobile ? '11px' : '12px') + ' Inter, Arial, sans-serif'; ctx.fillStyle = fillStyle; ctx.fillText(txt, curX, curY); curX += ctx.measureText(txt).width + (isMobile ? 4 : 6); }; const isStandard = activeBar.open !== undefined && activeBar.close !== undefined; if (isStandard) { drawText(`${this.symbol} · ${this.resolution}`, true, textColor); curX += isMobile ? 2 : 4;  const change = (activeBar.close - activeBar.open); const changePct = (change / activeBar.open * 100); const sign = change >= 0 ? '+' : ''; const changeText = `${sign}${change.toFixed(2)} (${sign}${changePct.toFixed(2)}%)`; if (isMobile) { drawText(changeText, false, ohlcColor); curX = 10; curY = 25; } const fmt = (v) => v !== undefined && v !== null ? v.toFixed(2) : '—'; drawText('O', false, labelColor); drawText(fmt(activeBar.open), false, ohlcColor); drawText('H', false, labelColor); drawText(fmt(activeBar.high), false, ohlcColor); drawText('L', false, labelColor); drawText(fmt(activeBar.low), false, ohlcColor); drawText('C', false, labelColor); drawText(fmt(activeBar.close), false, ohlcColor); if (!isMobile) { drawText(changeText, false, ohlcColor); } } else if (activeBar.yield !== undefined) { drawText(`${this.symbol} Yield Curve`, true, textColor); curX += 4; drawText('Maturity', false, labelColor); drawText(String(activeBar.maturity || activeBar.x), false, textColor); drawText('Yield', false, labelColor); drawText(`${activeBar.yield.toFixed(2)}%`, false, bullColor); } else if (activeBar.price !== undefined) { drawText(`${this.symbol} Option Strikes`, true, textColor); curX += 4; drawText('Strike', false, labelColor); drawText(String(activeBar.strike || activeBar.x), false, textColor); drawText('Price', false, labelColor); drawText(`$${activeBar.price.toFixed(2)}`, false, bullColor); } else if (activeBar.y !== undefined) { drawText(`${this.symbol}`, true, textColor); curX += 4; drawText('X', false, labelColor); drawText(String(activeBar.x), false, textColor); drawText('Y', false, labelColor); drawText(String(activeBar.y), false, bullColor); } } ctx.restore(); if (window.drawPriceScaleLabel) { window.drawPriceScaleLabel(this, ctx, minPrice, maxPrice); } if (window.ChartingAPI && window.ChartingAPI.drawPriceScaleDrawingLabel) { window.ChartingAPI.drawPriceScaleDrawingLabel(this, ctx, minPrice, maxPrice); } if (window.ChartingAPI && window.ChartingAPI.drawTimescaleDrawingLabel) { window.ChartingAPI.drawTimescaleDrawingLabel(this, ctx, chartH + totalPanelsH); } paneIndicators.forEach((x, pIdx) => { try { const panelY = chartH + pIdx * SUB_PANEL_H; const values = x.config.calculate(this.bars, x.ind.params); let minVal = Infinity; let maxVal = -Infinity; for (let i = startIndex; i <= endIndex; i++) { if (i < 0 || i >= values.length) continue; const val = values[i]; if (val == null) continue; if (Array.isArray(val)) { for (const v of val) { if (v != null && !isNaN(v)) { if (v < minVal) minVal = v; if (v > maxVal) maxVal = v; } } } else if (typeof val === 'object') { for (const k in val) { if (val[k] != null && !isNaN(val[k])) { if (val[k] < minVal) minVal = val[k]; if (val[k] > maxVal) maxVal = val[k]; } } } else { if (!isNaN(val)) { if (val < minVal) minVal = val; if (val > maxVal) maxVal = val; } } } if (minVal === Infinity || maxVal === -Infinity) { minVal = 0; maxVal = 100; } if (minVal === maxVal) { minVal -= 1; maxVal += 1; } else { const pad = (maxVal - minVal) * 0.05; minVal -= pad; maxVal += pad; } const range = maxVal - minVal; const toY = (val) => panelY + SUB_PANEL_H - ((val - minVal) / range) * SUB_PANEL_H; ctx.save(); ctx.beginPath(); ctx.rect(0, panelY, chartW, SUB_PANEL_H); ctx.clip(); x.config.render(ctx, this, values, { startIndex, endIndex, chartW, chartH: SUB_PANEL_H, panelY, toY }, x.ind.color); ctx.restore(); const levels = x.config.levels || []; levels.forEach(lvl => { const y = toY(lvl); if (y >= panelY && y <= panelY + SUB_PANEL_H) { ctx.save(); ctx.strokeStyle = isLight ? '#e0e3eb' : '#2a2e39'; ctx.lineWidth = 0.5; ctx.setLineDash([2, 4]); ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(chartW, y); ctx.stroke(); ctx.fillStyle = isLight ? '#707a8a' : '#9b9ba3'; ctx.font = '10px sans-serif'; ctx.textAlign = 'left'; ctx.textBaseline = 'middle'; ctx.fillText(lvl.toString(), chartW + 6, y); ctx.restore(); } }); ctx.save(); ctx.strokeStyle = isLight ? '#e0e3eb' : '#2a2e39'; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(chartW, panelY); ctx.lineTo(chartW, panelY + SUB_PANEL_H); ctx.stroke(); ctx.restore(); ctx.save(); ctx.fillStyle = isLight ? '#2a2e39' : '#d1d4dc'; ctx.font = '11px sans-serif'; ctx.textAlign = 'left'; ctx.textBaseline = 'top'; let displayValStr = ''; const hoverIdx = this.isMouseOver ? this.xToBar(this.mousePos.x) : endIndex; if (hoverIdx >= 0 && hoverIdx < values.length) { const hVal = values[hoverIdx]; if (hVal != null) { if (typeof hVal === 'number') { displayValStr = hVal.toFixed(2); } else if (Array.isArray(hVal)) { displayValStr = hVal.map(v => v != null ? v.toFixed(2) : '—').join(', '); } else if (typeof hVal === 'object') { displayValStr = Object.keys(hVal).map(k => `${k}: ${hVal[k] != null ? hVal[k].toFixed(2) : '—'}`).join(' '); } } } ctx.fillText(`${x.ind.name}: ${displayValStr}`, 10, panelY + 6); ctx.restore(); ctx.save(); ctx.strokeStyle = isLight ? '#e0e3eb' : '#2a2e39'; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(0, panelY + SUB_PANEL_H); ctx.lineTo(W, panelY + SUB_PANEL_H); ctx.stroke(); ctx.restore(); } catch (e) { console.error(`Error rendering sub-panel indicator ${x.ind.type}:`, e); } }); ctx.save(); const psSettings8 = window.ChartingAPI && typeof window.ChartingAPI.getPriceScaleSettings === 'function' ? window.ChartingAPI.getPriceScaleSettings() : null; const tsSettings8 = window.ChartingAPI && typeof window.ChartingAPI.getTimeScaleSettings === 'function' ? window.ChartingAPI.getTimeScaleSettings() : null; ctx.strokeStyle = psSettings8 && psSettings8.lineColor ? psSettings8.lineColor : (isLight ? '#e0e3eb' : '#2a2e39'); ctx.lineWidth = psSettings8 && psSettings8.lineWidth !== undefined ? psSettings8.lineWidth : 1; ctx.beginPath(); ctx.moveTo(chartW, 0); ctx.lineTo(chartW, chartH + totalPanelsH); ctx.stroke(); ctx.strokeStyle = tsSettings8 && tsSettings8.lineColor ? tsSettings8.lineColor : (isLight ? '#e0e3eb' : '#2a2e39'); ctx.lineWidth = tsSettings8 && tsSettings8.lineWidth !== undefined ? tsSettings8.lineWidth : 1; ctx.beginPath(); ctx.moveTo(0, chartH + totalPanelsH); ctx.lineTo(chartW, chartH + totalPanelsH); ctx.stroke(); ctx.restore(); if (this.isMouseOver && this.mousePos.x <= chartW && this.mousePos.y <= chartH + totalPanelsH) { ctx.save(); const crosshairSettings = window.ChartingAPI && typeof window.ChartingAPI.getCrosshairSettings === 'function' ? window.ChartingAPI.getCrosshairSettings() : null; const cColor = crosshairSettings && crosshairSettings.color  ? crosshairSettings.color  : (isLight ? '#000000' : '#ffffff'); const cWidth = crosshairSettings && crosshairSettings.width !== undefined  ? crosshairSettings.width  : 0.8; const cDash = crosshairSettings && crosshairSettings.dashArray  ? crosshairSettings.dashArray  : [3, 3]; ctx.strokeStyle = cColor; ctx.lineWidth = cWidth; ctx.setLineDash(cDash); const hoverBarIdx = this.xToBar(this.mousePos.x); const snappedX = this.barToX(hoverBarIdx); ctx.beginPath(); ctx.moveTo(snappedX, 0); ctx.lineTo(snappedX, chartH + totalPanelsH); ctx.stroke(); if (this.mousePos.y <= chartH) { ctx.beginPath(); ctx.moveTo(0, this.mousePos.y); ctx.lineTo(chartW, this.mousePos.y); ctx.stroke(); ctx.restore(); if (!crosshairSettings || crosshairSettings.showPriceBadge !== false) { window.DecodedScale.drawPriceBadge(this, ctx, minPrice, maxPrice, chartW); } } else { const relativeY = this.mousePos.y - chartH; const pIdx = Math.floor(relativeY / SUB_PANEL_H); if (pIdx >= 0 && pIdx < numPanels) { ctx.beginPath(); ctx.moveTo(0, this.mousePos.y); ctx.lineTo(chartW, this.mousePos.y); ctx.stroke(); } ctx.restore(); } if (!crosshairSettings || crosshairSettings.showTimeBadge !== false) { (this.horizontalScale || window.TimeScale).drawTimeBadge(this, ctx, chartH + totalPanelsH); } } if (this.smartLoader) { this.smartLoader.check(); } } } 
  window.BacktestxChart = BacktestxChart;
  
  // Expose TradingView widget compatibility wrapper
  window.widget = class widget {
    constructor(options) {
      this.options = options;
      let containerId = options.container;
      if (containerId instanceof HTMLElement) {
        if (!containerId.id) {
          containerId.id = 'chart_container_' + Math.random().toString(36).substr(2, 9);
        }
        containerId = containerId.id;
      }
      
      const isLight = String(options.theme).toLowerCase() === 'light';
      if (isLight) {
        document.body.classList.add('light-theme');
      } else {
        document.body.classList.remove('light-theme');
      }
      
      let resolution = String(options.interval || '1D');
      if (resolution === '1') resolution = '1m';
      else if (resolution === '2') resolution = '2m';
      else if (resolution === '3') resolution = '3m';
      else if (resolution === '5') resolution = '5m';
      else if (resolution === '10') resolution = '10m';
      else if (resolution === '15') resolution = '15m';
      else if (resolution === '30') resolution = '30m';
      else if (resolution === '45') resolution = '45m';
      else if (resolution === '60' || resolution === '1H') resolution = '1H';
      else if (resolution === '120' || resolution === '2H') resolution = '2H';
      else if (resolution === '180' || resolution === '3H') resolution = '3H';
      else if (resolution === '240' || resolution === '4H') resolution = '4H';
      
      const enabled = options.enabled_features || [];
      const disabled = options.disabled_features || [];
      const isEnabled = (feature, defaultVal = true) => {
        if (disabled.includes(feature)) return false;
        if (enabled.includes(feature)) return true;
        return defaultVal;
      };
      
      const mappedOptions = {
        symbol: options.symbol || 'BTCUSD',
        resolution: resolution,
        datafeed: options.datafeed,
        showTopToolbar: isEnabled('header_widget', true),
        showDrawingToolbar: isEnabled('left_toolbar', true),
        showAddCustomInterval: isEnabled('add_custom_interval', false),
        timezone: options.timezone || 'Etc/UTC',
        supported_resolutions: options.supported_resolutions
      };
      
      if (mappedOptions.showTopToolbar === false) {
        if (!document.getElementById('hide-top-toolbar-style')) {
          const style = document.createElement('style');
          style.id = 'hide-top-toolbar-style';
          style.textContent = '.top-toolbar { display: none !important; }';
          document.head.appendChild(style);
        }
      }
      if (mappedOptions.showDrawingToolbar === false) {
        if (!document.getElementById('hide-drawing-toolbar-style')) {
          const style = document.createElement('style');
          style.id = 'hide-drawing-toolbar-style';
          style.textContent = '.toolbar { display: none !important; }';
          document.head.appendChild(style);
        }
      }
      
      const chart = new window.BacktestxChart(containerId, mappedOptions);
      this.chartInstance = chart;
      window.chart = chart;
    }
    
    onChartReady(callback) {
      setTimeout(callback, 50);
    }
  };
  })(window);