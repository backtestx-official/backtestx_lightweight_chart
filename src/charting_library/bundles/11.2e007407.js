{
  const _0x528f2b = "8a07aa64eb0623f2";
  let _0xe0c57e = Math.floor(Math.random() * 413);
  const _0xd7f7f2 = Array.from({length: 3}, (_, i) => i + 413).reduce((acc, val) => acc + val, 0);
  if (_0xe0c57e < 0) { console.log(_0x528f2b); }
  (function() { return _0xd7f7f2 > 0 ? _0x528f2b : ""; })();
}
(function(window) { if (!window.ChartingAPI) return; window.ChartingAPI.registerIndicator('bb', { name: 'Bollinger Bands', type: 'overlay', params: { period: 20, mult: 2 }, defaultColor: '#9C27B0', calculate: function(bars, params) { const period = params.period || 20; const mult = params.mult || 2; const bb = new Array(bars.length).fill(null); if (bars.length < period) return bb; for (let i = period - 1; i < bars.length; i++) { let sum = 0; for (let j = 0; j < period; j++) { sum += bars[i - j].close; } const mid = sum / period; let variance = 0; for (let j = 0; j < period; j++) { const diff = bars[i - j].close - mid; variance += diff * diff; } const stdDev = Math.sqrt(variance / period); bb[i] = { mid: mid, upper: mid + mult * stdDev, lower: mid - mult * stdDev }; } return bb; }, render: function(ctx, chart, values, bounds, color) { const { startIndex, endIndex } = bounds; const themeColor = color || '#9C27B0'; ctx.save(); ctx.fillStyle = themeColor; ctx.globalAlpha = 0.05; ctx.beginPath(); let started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values[i]; if (v == null) continue; const x = chart.barToX(i); const y = chart.priceToY(v.upper); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } for (let i = Math.min(endIndex, chart.bars.length - 1); i >= startIndex; i--) { const v = values[i]; if (v == null) continue; const x = chart.barToX(i); const y = chart.priceToY(v.lower); ctx.lineTo(x, y); } ctx.closePath(); ctx.fill(); ctx.restore(); ctx.lineWidth = 1; ctx.beginPath(); ctx.strokeStyle = themeColor; started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values[i]; if (v == null) { started = false; continue; } const x = chart.barToX(i); const y = chart.priceToY(v.upper); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } ctx.stroke(); ctx.beginPath(); ctx.strokeStyle = themeColor; started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values[i]; if (v == null) { started = false; continue; } const x = chart.barToX(i); const y = chart.priceToY(v.lower); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } ctx.stroke(); ctx.beginPath(); ctx.strokeStyle = '#FF9800';  ctx.lineWidth = 1.2; started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values[i]; if (v == null) { started = false; continue; } const x = chart.barToX(i); const y = chart.priceToY(v.mid); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } ctx.stroke(); } }); })(window);